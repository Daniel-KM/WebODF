<?xml version="1.0" encoding="UTF-8"?>
<office:document xmlns:office="urn:oasis:names:tc:opendocument:xmlns:office:1.0" xmlns:style="urn:oasis:names:tc:opendocument:xmlns:style:1.0" xmlns:text="urn:oasis:names:tc:opendocument:xmlns:text:1.0" xmlns:fo="urn:oasis:names:tc:opendocument:xmlns:xsl-fo-compatible:1.0" office:version="1.3" office:mimetype="application/vnd.oasis.opendocument.text">
<office:automatic-styles>
<style:page-layout style:name="pm1"><style:page-layout-properties fo:page-width="21cm" fo:page-height="29.7cm" fo:margin-top="2cm" fo:margin-bottom="2cm" fo:margin-left="2cm" fo:margin-right="2cm"><style:columns fo:column-count="2" fo:column-gap="0.7cm"/></style:page-layout-properties></style:page-layout>
<style:style style:name="Sect1" style:family="section"><style:section-properties><style:columns fo:column-count="3" fo:column-gap="0.5cm"/></style:section-properties></style:style>
</office:automatic-styles>
<office:styles>
<style:style style:name="Standard" style:family="paragraph"><style:paragraph-properties fo:margin-bottom="0.2cm"/><style:text-properties fo:font-size="11pt"/></style:style>
</office:styles>
<office:master-styles><style:master-page style:name="Standard" style:page-layout-name="pm1"/></office:master-styles>
<office:body><office:text><text:p text:style-name="Standard">Paragraph 1. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 2. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 3. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 4. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 5. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 6. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 7. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 8. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 9. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 10. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 11. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 12. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 13. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 14. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 15. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 16. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 17. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 18. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 19. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 20. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 21. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 22. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 23. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 24. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 25. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 26. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 27. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 28. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 29. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 30. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 31. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 32. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 33. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 34. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 35. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 36. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 37. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 38. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 39. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 40. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 41. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 42. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 43. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 44. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 45. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 46. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 47. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 48. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 49. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 50. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 51. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 52. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 53. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 54. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 55. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 56. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 57. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 58. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 59. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 60. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 61. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 62. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 63. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 64. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 65. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 66. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 67. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 68. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 69. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 70. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 71. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 72. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 73. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 74. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 75. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 76. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 77. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 78. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 79. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:p text:style-name="Standard">Paragraph 80. The text of the page goes on and on, of a length that fills a column and then the next one, so that the pages are broken where the last column of a page is full.</text:p><text:section text:style-name="Sect1" text:name="S1"><text:p text:style-name="Standard">Section paragraph 1. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 2. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 3. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 4. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 5. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 6. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 7. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 8. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 9. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 10. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 11. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 12. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 13. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 14. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 15. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 16. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 17. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 18. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 19. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p><text:p text:style-name="Standard">Section paragraph 20. Two columns of a section of the text, that run from the foot of one to the head of the next.</text:p></text:section></office:text></office:body>
</office:document>